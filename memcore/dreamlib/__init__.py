"""dreams — a standalone, non-forcing memory-consolidation utility.

Reflect on a coding-agent SESSION or an existing memory STORE, and write durable
facts back into any memory directory (a mymemories partition or a plain folder).
Agent-agnostic (Claude Code / Codex), store-agnostic.
"""
__version__ = "1.0.0"
